import React from 'react';

const Outfit = (props) => {
    return (
        <div className="outfit">
            <h2>outfit props go here props.outfit.data</h2>
        </div>
    );
};

export default Outfit;